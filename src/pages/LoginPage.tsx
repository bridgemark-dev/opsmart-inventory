// Stub — Login is handled by the existing login.html / Supabase Auth UI.
// This component exists to satisfy the router; it redirects to the Supabase
// hosted login or the existing login page.
export default function LoginPage() {
  return null;
}
